/**
 * Free-shipping threshold, in whole kronor. This mirrors the real published
 * shipping policy ("Fri frakt på alla beställningar över 199 kr" on the
 * Leverans page), so the USP row and announcement bar state the true figure.
 * Update here if the policy changes.
 */
export const FREE_SHIPPING_THRESHOLD = 199;
