/**
 * The four jewelry category collection handles, the ones the primary nav,
 * homepage category tiles, and the bundle builder scope to (excluding the
 * accessory and curated collections). Pure config so it's a single source of
 * truth across the mock and live data layers.
 */
export const JEWELRY_COLLECTION_HANDLES = ["orhangen", "halsband", "armband", "ovrigt"];
